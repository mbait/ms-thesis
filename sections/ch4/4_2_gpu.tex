\section{Ускорение на графическом процессоре средствами SYCL/AdaptiveCpp}

\subsection{Слой compute/: API и архитектура}

Слой \texttt{compute/} инкапсулирует все обращения к GPU-стеку.
Физическое ядро взаимодействует только с этим слоем и никогда не использует SYCL напрямую.
Это позволяет сменить GPU-технологию, не затрагивая физическую логику.

Основные компоненты слоя:

\paragraph{\texttt{Device}.}
Представляет вычислительное устройство.
Устройство выбирается статическими методами \texttt{default\_gpu()} и \texttt{default\_cpu()};
через него создаётся \texttt{Stream}.

\paragraph{\texttt{Stream}.}
Обёртка над \texttt{sycl::queue} в режиме \textit{in-order}.
Все операции, поставленные в один \texttt{Stream}, выполняются строго последовательно.
Метод \texttt{wait()} блокирует CPU до завершения всех поставленных задач.

\paragraph{\texttt{Buffer<T>}.}
Буфер в USM-памяти устройства.
Предоставляет методы \texttt{upload} и \texttt{download} для асинхронной передачи
данных CPU$\leftrightarrow$GPU; при уничтожении буфера память освобождается автоматически.

\paragraph{\texttt{parallel\_for(s, n, f)}.}
Запускает GPU-ядро: лямбда \texttt{f(size\_t i)} вызывается
для каждого $i \in [0, n)$ параллельно.
Ядро должно быть тривиально копируемым; внутри допустимы только device-safe операции.

\paragraph{\texttt{reduce\_sum}, \texttt{sort\_by\_key}.}
Детерминированные примитивы: редукция возвращает одно и то же значение
при одинаковых входных данных на одном устройстве.

\subsection{Жизненный цикл данных}

Все горячие данные живут в памяти устройства между шагами симуляции:
ни позиции, ни скорости тел не переносятся на CPU при каждом шаге.
Единственная синхронизация CPU/GPU per frame~--- чтение счётчика кандидатных пар
после широкой фазы (одно 4-байтное чтение).

\begin{enumerate}
  \item При инициализации сцены данные копируются CPU$\to$GPU один раз (\texttt{upload}).
  \item На каждом шаге симуляции GPU-ядра модифицируют данные in-place.
  \item Для сохранения траектории (файл \texttt{.trajectory}) данные
    выгружаются GPU$\to$CPU один раз на два кадра; это единственная загрузка шины.
  \item По завершении симуляции финальное состояние выгружается для
    вычисления контрольного хеша.
\end{enumerate}

\subsection{Многобэкендность AdaptiveCpp}

AdaptiveCpp компилирует одни и те же C++20-исходники для нескольких бэкендов:

\begin{table}[h]
\centering
\caption{Бэкенды AdaptiveCpp и применяемые технологии}
\label{tab:backends}
\begin{tabular}{lll}
\toprule
\textbf{Пресет CMake} & \textbf{Бэкенд} & \textbf{Оборудование} \\
\midrule
\texttt{cuda}    & NVIDIA CUDA PTX  & NVIDIA RTX / Tesla / A-серия \\
\texttt{hip}     & AMD HIP/ROCm     & AMD RDNA / CDNA \\
\texttt{omp}     & OpenMP CPU       & Любой многоядерный CPU \\
\bottomrule
\end{tabular}
\end{table}

Выбор бэкенда влияет только на флаги компиляции, но не на исходный код движка.
В CI-матрице собираются и тестируются все три варианта на каждом pull request.

\subsection{Детерминизм на уровне примитивов}

Стандартная атомарная операция \texttt{atomicAdd} в CUDA/HIP не детерминирована:
результат зависит от порядка прибытия нитей.
Для детерминированных редукций в \texttt{compute/} реализован примитив
\texttt{reduce\_sum}, использующий дерево редукции с фиксированным порядком операций.

Аналогично, широкая фаза после построения дерева выполняет детерминированную
сортировку кандидатных пар, прежде чем передать их в узкую фазу.

Благодаря этим мерам одна и та же сцена даёт побитово идентичный результат
при каждом запуске на одном устройстве и одной сборке.
Данное свойство верифицируется в CI через «golden trace» (раздел~5.1).

\subsection{Выводы по разделу}

Слой \texttt{compute/} обеспечивает три ключевых свойства системы:
изоляцию физической логики от GPU API (что позволяет тестировать её на CPU),
поддержку нескольких GPU-платформ из единого исходного кода,
и детерминизм GPU-вычислений через специализированные примитивы.

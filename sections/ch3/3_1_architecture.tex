\section{Архитектура программного комплекса «Модейлер»}

\subsection{Общее описание платформы}

«Модейлер» — кроссплатформенная программная система для интерактивного моделирования и симуляции роботизированных устройств. Платформа предназначена для инженеров-разработчиков, исследователей и специалистов по автоматизации, решающих задачи верификации кинематических и динамических характеристик роботов без физического прототипирования.

В области промышленной робототехники задачи, для которых применяется «Модейлер», охватывают проектирование роботизированных ячеек, отладку управляющих алгоритмов в режиме программного замкнутого контура (SIL), а также подготовку траекторий движения. В сфере исследовательских и образовательных применений платформа обеспечивает возможность экспериментов с моделями на основе описания роботов в формате URDF\footnote{Unified Robot Description Format — стандартный XML-формат, используемый в экосистеме ROS.} без необходимости развёртывания реального оборудования.

На рисунке~\ref{fig:modeuler_screenshot} показан пример рабочей сцены в «Модейлере»: конвейерная линия с двумя промышленными манипуляторами.

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{figures/images/photo_2026-03-04_15-25-47.jpg}
\caption{Рабочая сцена в «Модейлере»: конвейерная линия с двумя манипуляторами, выполняющими операции загрузки и разгрузки коробок}
\label{fig:modeuler_screenshot}
\end{figure}

\subsection{Многоуровневая архитектура}

Архитектура «Модейлера» организована по принципу строгого разделения ответственности.
Рисунок~\ref{fig:modeuler_architecture} иллюстрирует взаимодействие компонентов системы.
Ключевой особенностью является подключение физического движка \textit{dyphur}
как \textbf{внешней независимой библиотеки}: он распространяется отдельно
под лицензией Apache~2.0 и взаимодействует с ядром «Модейлера» через C++~API,
не разделяя с ним Vulkan-устройство рендеринга.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  font=\small,
  layer/.style={
    draw, rounded corners=4pt,
    minimum width=7.5cm, minimum height=1.1cm,
    align=center, text width=7cm
  },
  extlib/.style={
    draw, rounded corners=4pt, dashed,
    minimum width=4cm, minimum height=1.1cm,
    align=center, text width=3.7cm, fill=blue!8
  },
  grouplabel/.style={font=\footnotesize\itshape, text=gray},
  arr/.style={-{Stealth[length=6pt]}, thick},
  extarr/.style={-{Stealth[length=6pt]}, thick, dashed},
]

% Модейлер layers
\node[layer, fill=orange!10] (ui_win) at (-1.8,5.2) {WPF / .NET~10 (Windows)};
\node[layer, fill=orange!10, minimum width=3.5cm, text width=3.2cm]
                             (ui_gtk) at (3.3,5.2)  {GTK~4 / C (Linux)};
\node[layer, fill=yellow!10] (core)   at (0,3.7)   {Программное ядро (C~ABI)\\{\footnotesize modeuler.h, Vulkan-устройство, VSG}};
\node[layer, fill=green!10]  (scene)  at (0,2.3)   {Слой сцены и рендеринга\\{\footnotesize VulkanSceneGraph, vsgXchange, urdfdom}};

% dyphur — внешняя библиотека
\node[extlib]                (dyphur) at (5.5,2.3)  {dyphur\\{\footnotesize SYCL~2020 / AdaptiveCpp\\CUDA · HIP · OpenMP}};

% Стрелки внутри Модейлера
\draw[arr] (scene.north) -- (core.south);
\draw[arr] (core.north) -- ++(0,0.45) -| (ui_win.south);
\draw[arr] (core.north) -- ++(0,0.45) -| (ui_gtk.south);

% Стрелка к dyphur (двунаправленная: вызов + возврат трансформаций)
\draw[extarr] (core.east) -- ++(0.45,0) |- (dyphur.north)
    node[midway, above, font=\footnotesize] {C++ API};
\draw[extarr] (dyphur.south) -- ++(0,-0.4) -| ++(-0.45,0) |- (scene.east)
    node[pos=0.6, right, font=\footnotesize] {трансформации};

% Подписи
\node[grouplabel, left=0.2cm of scene]  {§\,3.1.3};
\node[grouplabel, right=0.3cm of dyphur, yshift=8pt] {гл.\,4};

\end{tikzpicture}
\caption{Архитектура «Модейлера» и место библиотеки dyphur}
\label{fig:modeuler_architecture}
\end{figure}

\textbf{Уровень пользовательского интерфейса.}
Для обеспечения кроссплатформенности реализованы два независимых приложения.
Приложение для Windows построено на основе .NET~10 с использованием WPF,
Fluent.Ribbon и AvalonDock.
Приложение для Linux реализовано на~C с использованием GTK~4.
Оба варианта взаимодействуют с ядром исключительно через C~ABI.

\textbf{Уровень программного ядра} предоставляет стабильный интерфейс
через заголовочный файл~\texttt{modeuler.h} (C-совместимый).
Внутри ядра располагается менеджер Vulkan-устройства~--- единственный экземпляр
\texttt{vsg::Instance} и связанное с ним \texttt{vsg::Device}, используемые
для рендеринга через VSG. Физические вычисления dyphur выполняются
на том же GPU через независимый SYCL-контекст, не пересекаясь с
Vulkan-потоком команд.

\textbf{Уровень сцены и рендеринга} реализован на основе VulkanSceneGraph~(VSG).
VSG организует трёхмерную сцену в виде ориентированного ациклического графа (DAG).
Для загрузки 3D-геометрии используется плагин~vsgXchange;
для разбора описаний роботов~--- библиотека~urdfdom.

\textbf{Физический движок dyphur} подключается как статически компонуемая библиотека.
Ядро «Модейлера» вызывает C++~API dyphur для выполнения шага симуляции,
получает обновлённые трансформации тел и применяет их к VSG-узлам сцены.
Такая граница позволяет независимо развивать и обновлять оба проекта:
«Модейлер» не зависит от конкретного GPU-стека dyphur и может быть
перенесён на другой физический движок без изменения архитектуры.

\subsection{Кинематическая модель робота}

Описание робота в формате URDF задаёт дерево звеньев (\textit{links}) и сочленений (\textit{joints}).
Каждое звено несёт информацию о визуальной геометрии, коллизионной геометрии
и инерционных характеристиках; каждое сочленение определяет тип кинематической
пары, её ось и пределы перемещения.
При загрузке URDF ядро «Модейлера» транслирует описание в структуры dyphur:
каждое звено становится телом (\texttt{BodyParams}), каждое сочленение~---
записью в \texttt{JointParams}, после чего данные загружаются на GPU.

\subsection{Программный интерфейс ядра}
\label{sec:core_api}

Программный интерфейс ядра спроектирован по принципу минимальной поверхности:
пользовательский код работает только с непрозрачными дескрипторами и узким набором функций.
Публичный C~ABI включает четыре группы функций: инициализация и завершение;
управление сценой (создание, загрузка URDF); управление окном отображения;
шаг симуляции и рендеринг кадра.

Функция \texttt{modeuler\_physics\_step} вызывает шаг dyphur с фиксированным
шагом $\Delta t$ (типично 1--2\,мс для реального времени), считывает
обновлённые трансформации тел и применяет их к VSG-узлам,
после чего \texttt{modeuler\_run\_frame} генерирует новый кадр.
Привязки для управляемых языков формируются инструментом~ClangSharp,
который генерирует код P/Invoke на~C\# для интеграции с WPF-приложением.

\subsection{Взаимодействие с ROS~2}

Интеграция с ROS~2 реализуется на двух уровнях.
На уровне форматов данных: «Модейлер» использует URDF~--- официальный формат
описания роботов в экосистеме ROS, а система координат соответствует конвенции
фреймов~TF2.
На уровне обмена данными: ядро «Модейлера» публикует состояние сочленений,
одометрию и показания сенсоров через топики ROS~2, используя данные,
полученные от dyphur по завершении шага симуляции.
Это позволяет использовать «Модейлер» в качестве имитатора для управляющего
программного обеспечения, работающего в штатной среде ROS~2 без изменений кода.
